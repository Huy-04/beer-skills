# Discovery Report: <Feature Name>

**Date**: <YYYY-MM-DD>
**Feature**: <feature-slug>
**Route**: `feature` | `small-fix` | `debug-escalation`

## 1. Institutional Learnings

- Critical patterns applied: `<summary or none>`
- Similar prior learnings: `<files or none>`
- Notes that must shape this plan: `<summary>`

## 2. Current Reality

### Relevant Files

| File | Why it matters |
|---|---|
| `path/to/file` | `<reason>` |

### Existing Pattern to Reuse

- `<pattern>` in `path/to/file`
- `<pattern>` in `path/to/file`

### Constraints

- Runtime/framework: `<constraint>`
- Tests/build checks: `<constraint>`
- Integration/data/auth concerns: `<constraint>`

## 3. Route-Specific Findings

### Feature Route

- architecture boundary: `<summary>`
- existing implementation shape: `<summary>`
- external research needed: `<yes/no and why>`

### Small Direct-Fix Route

- directly implicated files: `<summary>`
- nearby pattern to follow: `<summary>`
- why this stays small: `<summary>`

### Debug-Escalation Route

- root cause: `<verbatim sentence from debugging>`
- failing command: `<command>`
- repair boundary: `<summary>`

Remove sections that do not apply to the active route.

## 4. Open Questions

- [ ] `<question that still matters>`

## 5. Planning Input Summary

- what exists today: `<summary>`
- what needs to change: `<summary>`
- biggest risk: `<summary>`
